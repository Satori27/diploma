version: "2"
sql:
  - engine: "postgresql"
    schema: "../../migrations/*.sql"
    queries: "./orders.sql"
    gen:
      go:
        package: "db"
        out: "queries"
        sql_package: "pgx/v5"

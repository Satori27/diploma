apiVersion: v1
kind: ConfigMap
metadata:
  name: checkout-service
  namespace: default
data:
  DB_HOST: "postgres.database.svc.cluster.local"
  DB_PORT: "5432"
  KAFKA_CONN: "kafka:9092"
  BATCH_SIZE: "1000"
  DB_TIMEOUT_MS: "5000"
  TICKER_DURATION_MS: "500"
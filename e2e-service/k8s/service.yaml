apiVersion: v1
kind: Service
metadata:
  name: e2e-service
  labels:
    app: e2e-service
spec:
  selector:
    app: e2e-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP